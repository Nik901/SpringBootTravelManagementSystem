A Spring Boot backend project that exposes RESTful APIs to handle user registration and stores data in a MySQL database. 
The project is designed to work with an Angular frontend registration page, where form data is submitted via API calls.
Build Tool: Maven
